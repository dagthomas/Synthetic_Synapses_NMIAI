# Close the Gap: 0.7758 → 0.7824+

**Date:** 2026-03-20 (updated)
**Current score:** 0.7758 | **Leader:** 0.7824 | **Gap:** 0.0066
**Formula:** 70% detection mAP@0.5 + 30% classification mAP@0.5

---

## Key Findings from Experiments

### Train-set mAP is saturated at 0.978
All inference-time parameter combinations score 0.9778-0.9780 on train.
The gap to leader is purely about **test-set generalization**.

### Sweep Results (33 YOLO configs × 3 classifier configs)
- **Best YOLO config:** sz1536, conf=0.001, iou=0.6, TTA=True (det=0.9839)
- **Higher resolution (1920) HURTS:** det=0.9806 (model trained at 1280)
- **TTA is critical:** +0.001 detection mAP vs no TTA
- **NMS IoU 0.6 is optimal** (0.5 and 0.7 both slightly worse)
- **Classifier params barely matter:** <0.0002 variation across all configs

### What doesn't help on train
- SAHI tiling: -0.005 to -0.009 (too many conflicting detections)
- Old+New YOLO ensemble: -0.005 (old model too noisy)
- Best+Last checkpoint ensemble: +0.000 (models too similar)
- Score compression: -0.001 to -0.002 (but might help test generalization)
- Soft-NMS: +0.0001 (negligible)

### Best classifier blend
`yolo_trust`: agree = conf × (0.7 + 0.3×prob), disagree = conf × prob × 0.5

---

## Active Improvements

### 1. ConvNeXt-V2 Tiny Classifier (TRAINING NOW)
- 28.1M params vs 15.2M nano (~2x capacity)
- FP16 safetensors: ~54 MB (total zip ~185 MB)
- Expected: better fine-grained classification on test products
- Status: Training 80 epochs, currently at epoch 23, 97.9% accuracy

### 2. YOLOv8x v2 Training (QUEUED - after classifier)
- 200 epochs (was 120)
- copy_paste=0.2 (was 0.1), degrees=10, translate=0.15
- scale=0.6, erasing=0.3, close_mosaic=40
- Goal: better generalization through stronger augmentation

---

## Completed Improvements (from earlier today)

- [x] YOLO retrained with mixup=0.0 (120 epochs) — det mAP +0.011 on train
- [x] SAHI tiling implemented and tested — HURTS, disabled
- [x] Background reject class (357 classes) — marginal help (+0.004 cls_mAP)
- [x] yolo_trust blending — +0.0002 cls_mAP
- [x] 33-config YOLO parameter sweep
- [x] 7-config classifier parameter sweep
- [x] Soft-NMS tested — negligible
- [x] Multi-checkpoint ensemble tested — no gain
- [x] Old+new YOLO diversity ensemble — worse

---

## Submission Strategy

### Already submitted
- `submission_safe.zip` — new YOLO + TTA + 357-cls + default blend

### Ready to submit
- `submission_sub6_yolotrust.zip` — same + yolo_trust blend
- `submission_sub7_alpha.zip` — same + yolo_trust + score^1.1

### Pending (after training)
- **Sub 8:** New YOLO + TTA + ConvNeXt-V2 tiny classifier + yolo_trust
- **Sub 9:** YOLO v2 (improved augmentation) + ConvNeXt-V2 tiny + yolo_trust

---

## Important Notes

- `ensemble-boxes` is pre-installed in sandbox (can use WBF)
- Public leaderboard ≠ final ranking (private test set)
- 3 submissions/day, reset at midnight UTC
- "Select for final" lets us choose best submission for private evaluation
