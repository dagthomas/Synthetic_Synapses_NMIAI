# grocery-bot-gpu dependencies
# Install core: pip install -r requirements.txt
# For PyTorch with CUDA, install separately via https://pytorch.org

# --- Core dependencies (always needed) ---

# PyTorch with CUDA support
# Install via: pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.0

numpy>=1.24

websockets>=12.0

# --- Optional dependencies (uncomment as needed) ---

# scipy: MAPF planner bot-to-task assignment (planner.py: linear_sum_assignment)
# scipy>=1.11  # optional: MAPF planner

# triton: torch.compile Triton kernel fusion backend (gpu_beam_search.py)
# triton>=3.0  # optional: torch.compile acceleration

# numba: legacy Numba CUDA kernel simulation (gpu_sim.py)
# numba>=0.58  # optional: Numba CUDA GPU sim kernels

# playwright: automated browser token fetching (fetch_token.py)
# playwright>=1.40  # optional: browser-based token fetching

# psycopg2: PostgreSQL replay database recording (synthetic_optimize.py)
# psycopg2-binary>=2.9  # optional: PostgreSQL database recording
